# kichov1

## Prompt for this workspace pass

Goal: prepare the university practice repository without pushing anything to GitHub.

Rules:
- Read the existing DOCX/PDF practice materials first.
- Keep DOCX files local only; they must not be sent to GitHub.
- Allow PDF files and extracted C++ code files to be sent to GitHub.
- Split code from the reports into separate files for day 1 and day 2.
- Save this note as `kichov1` so the next pass can quickly understand what was changed.

## What I found

- Tracked already: `дневник.pdf`, `Отчёт_1день_Нурасыл.pdf`.
- New PDF files in the workspace: `дневник_(2).pdf`, `Отчёт_2день_Шакир-1.pdf`.
- DOCX source files are inside `практика нурасыла 67 копия/`.
- Day 1 code is in `Отчёт_1день_Нурасыл.docx`.
- Day 2 code is in `Отчёт_2день_Шакир-1.docx`.

## Change log

- Added `.gitignore` so only PDF files, C++ code files, folders, and `.gitignore` are visible to Git.
- Added `1 день/` with separate C++ files for day 1 assignments.
- Added `2 день/` with separate C++ files for day 2 assignments.
- Did not run `git add`, commit, push, or create a pull request.
